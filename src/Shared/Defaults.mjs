/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class TeqFw_Plugin_Shared_Defaults {

    NAME = '@teqfw/plugin'; // plugin's node in 'teqfw.json' & './cfg/local.json'

    constructor() {
        Object.freeze(this);
    }
}
